import 'package:flutter/material.dart';

class Rec_Facial extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Icon(
        Icons.security,
        color: Colors.white,
      ),
    );
  }
}
